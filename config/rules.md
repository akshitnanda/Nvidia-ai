# Repository Rules

- Prefer small, reviewable changes over giant rewrites.
- Preserve secrets and never overwrite `.env` or private key files.
- Default to preview-first edits unless the user explicitly enables apply mode.
- Add or update tests when behavior changes.
- Keep logs, run artifacts, and memory local to this machine.
- Favor modular Python files over monoliths.

